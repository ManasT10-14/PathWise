---
phase: 01-full-platform-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/theme/app_theme.dart
  - lib/theme/glass_card.dart
  - lib/theme/gradient_background.dart
  - lib/widgets/skeleton_loader.dart
  - lib/widgets/error_state.dart
  - lib/widgets/empty_state.dart
  - lib/widgets/ai_progress_indicator.dart
  - lib/widgets/timeline_node.dart
  - lib/widgets/confidence_badge.dart
  - lib/main.dart
  - lib/screens/login_screen.dart
  - lib/screens/home_mode_screen.dart
  - lib/screens/ai_guidance_screen.dart
  - lib/screens/roadmap_detail_screen.dart
  - lib/screens/experts_screen.dart
  - lib/screens/expert_detail_screen.dart
  - lib/screens/book_consultation_screen.dart
  - lib/screens/consultation_detail_screen.dart
  - lib/screens/expert_home_screen.dart
  - lib/screens/profile_screen.dart
  - lib/screens/review_submit_screen.dart
  - lib/screens/admin_dashboard_screen.dart
  - lib/screens/user_main_shell.dart
  - lib/screens/role_router.dart
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - ADAPT-06
  - EXP-01
  - EXP-02
  - EXP-03

must_haves:
  truths:
    - "All screens render with frosted glass cards over gradient mesh backgrounds"
    - "Dark mode toggle switches all screens between light and dark color schemes"
    - "Onboarding wizard has 4 steps with animated page transitions and progress indicator"
    - "Roadmap displays as vertical timeline with connected nodes and pulsing current position"
    - "AI analysis screen shows 4-step progress storytelling with animated transitions"
    - "Expert marketplace has domain filter chips, price range slider, rating filter, and sort options"
    - "All data-loading screens show shimmer skeleton loading instead of CircularProgressIndicator"
    - "All screens have polished error states with retry button and empty states with guidance text"
    - "Skill gaps display confidence scores as colored badges (red/yellow/green)"
    - "Micro-interactions: card entrance animations, button feedback, progress ring fills"
  artifacts:
    - path: "lib/theme/app_theme.dart"
      provides: "Light and dark ThemeData with glassmorphism color tokens"
      contains: "class AppTheme"
    - path: "lib/theme/glass_card.dart"
      provides: "Reusable glassmorphism card widget"
      contains: "class GlassCard"
    - path: "lib/theme/gradient_background.dart"
      provides: "Gradient mesh background widget"
      contains: "class GradientBackground"
    - path: "lib/widgets/skeleton_loader.dart"
      provides: "Shimmer skeleton loading widget"
      contains: "class SkeletonLoader"
    - path: "lib/widgets/ai_progress_indicator.dart"
      provides: "4-step AI analysis storytelling widget"
      contains: "class AiProgressIndicator"
    - path: "lib/widgets/timeline_node.dart"
      provides: "Vertical timeline node components"
      contains: "class TimelineNode"
    - path: "lib/widgets/confidence_badge.dart"
      provides: "Colored confidence score badge"
      contains: "class ConfidenceBadge"
  key_links:
    - from: "lib/main.dart"
      to: "lib/theme/app_theme.dart"
      via: "ThemeData assignment"
      pattern: "AppTheme"
    - from: "lib/screens/roadmap_detail_screen.dart"
      to: "lib/widgets/timeline_node.dart"
      via: "Widget usage"
      pattern: "TimelineNode"
    - from: "lib/screens/ai_guidance_screen.dart"
      to: "lib/widgets/ai_progress_indicator.dart"
      via: "Widget usage"
      pattern: "AiProgressIndicator"
    - from: "lib/screens/experts_screen.dart"
      to: "lib/theme/glass_card.dart"
      via: "Widget usage"
      pattern: "GlassCard"
---

<objective>
Transform all 14 Flutter screens from stock Material Design to a polished glassmorphism design system with animations, dark mode, skeleton loading, an onboarding wizard, roadmap timeline visualization, expert marketplace filters, and micro-interactions.

Purpose: The UI is the first impression. Without visual polish, the AI backend feels like a CLI tool. This plan creates the "wow" factor that makes Pathwise a portfolio-grade app and prevents user abandonment during the 6-8 second AI analysis wait.

Output: Complete design system (theme, glass cards, gradients), 7 reusable widgets, all 14 screens overhauled with glassmorphism, animations, skeleton loading, and error/empty states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-full-platform-overhaul/01-RESEARCH.md
@lib/main.dart
@lib/providers/app_services.dart
@lib/models/roadmap.dart
@lib/models/expert.dart
@lib/models/consultation.dart
@lib/models/app_user.dart
@pubspec.yaml

<interfaces>
<!-- Existing service patterns that UI code must continue to use -->

From lib/providers/app_services.dart:
```dart
class AppServices extends InheritedWidget {
  final AuthService auth;
  final UserRepository users;
  final ExpertRepository experts;
  final ConsultationRepository consultations;
  final RoadmapRepository roadmaps;
  final ReviewRepository reviews;
  final AiRoadmapService ai;
  final PaymentService payments;
}
extension AppServicesX on BuildContext {
  AppServices get svc => AppServices.of(this);
}
```

From lib/models/expert.dart:
```dart
class Expert {
  final String domain;
  final double rating;
  final num pricePerSession;
  final bool isVerified;
  final List<String> skills;
  final int totalReviews;
}
```

From lib/models/roadmap.dart:
```dart
class RoadmapStage {
  final String level;
  final String title;
  final List<String> tasks;
  final List<String> resources;
}
class Roadmap {
  List<RoadmapStage> get structuredStages { ... }
  final Map<String, double> stageProgress;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Flutter dependencies — dio, flutter_animate, lottie, google_fonts, shimmer</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>
    Add the following dependencies to the `dependencies:` section of `pubspec.yaml`, after the existing `razorpay_flutter: ^1.4.0` line:

    ```yaml
      dio: ^5.9.2
      flutter_animate: ^4.5.2
      lottie: ^3.3.2
      google_fonts: ^6.2.1
      shimmer: ^3.0.0
    ```

    Do NOT modify any existing dependencies. Do NOT change the sdk constraint. Do NOT change dev_dependencies.
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "dio:\|flutter_animate:\|lottie:\|google_fonts:\|shimmer:" pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - File `pubspec.yaml` contains `dio: ^5.9.2` (grep-verifiable)
    - File `pubspec.yaml` contains `flutter_animate: ^4.5.2` (grep-verifiable)
    - File `pubspec.yaml` contains `lottie: ^3.3.2` (grep-verifiable)
    - File `pubspec.yaml` contains `google_fonts: ^6.2.1` (grep-verifiable)
    - File `pubspec.yaml` contains `shimmer: ^3.0.0` (grep-verifiable)
    - File `pubspec.yaml` still contains `razorpay_flutter: ^1.4.0` (existing dep preserved)
    - File `pubspec.yaml` still contains `firebase_core: ^3.8.1` (existing dep preserved)
  </acceptance_criteria>
  <done>All 5 new Flutter packages added to pubspec.yaml without disturbing existing dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Design system foundation — AppTheme, GlassCard, GradientBackground</name>
  <files>
    lib/theme/app_theme.dart,
    lib/theme/glass_card.dart,
    lib/theme/gradient_background.dart
  </files>
  <read_first>
    lib/main.dart,
    pubspec.yaml,
    .planning/phases/01-full-platform-overhaul/01-RESEARCH.md
  </read_first>
  <action>
    Create `lib/theme/` directory with 3 files:

    **lib/theme/app_theme.dart** (per UI-03):
    - `class AppTheme` with static methods `static ThemeData light()` and `static ThemeData dark()`
    - Light theme:
      - `ColorScheme.fromSeed(seedColor: Color(0xFF2E7D32), brightness: Brightness.light)` (keep existing green seed)
      - `scaffoldBackgroundColor: Color(0xFFF5F7FA)` (light cool gray)
      - `appBarTheme: AppBarTheme(backgroundColor: Colors.transparent, elevation: 0, scrolledUnderElevation: 0)`
      - `cardTheme: CardTheme(elevation: 0, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)))`
      - `textTheme` using `GoogleFonts.interTextTheme()` for body, `GoogleFonts.poppinsTextTheme()` for display/headline
      - `useMaterial3: true`
    - Dark theme:
      - `ColorScheme.fromSeed(seedColor: Color(0xFF4CAF50), brightness: Brightness.dark)` (slightly brighter green for dark)
      - `scaffoldBackgroundColor: Color(0xFF0D1117)` (dark GitHub-style)
      - Same appBar/card/text theming adapted for dark
    - Static color constants for glassmorphism:
      - `static const glassLight = Color(0x26FFFFFF)` (white 15% opacity)
      - `static const glassDark = Color(0x26000000)` (black 15% opacity)
      - `static const glassBorder = Color(0x33FFFFFF)` (white 20% opacity)
      - `static Color glassColor(BuildContext context) => Theme.of(context).brightness == Brightness.dark ? glassDark : glassLight`

    **lib/theme/glass_card.dart** (per UI-01):
    - `class GlassCard extends StatelessWidget`
    - Constructor: `const GlassCard({super.key, required this.child, this.blur = 10, this.opacity = 0.15, this.borderRadius = 16, this.padding = const EdgeInsets.all(16)})`
    - Build: `ClipRRect` -> `BackdropFilter(filter: ImageFilter.blur(sigmaX: blur, sigmaY: blur))` -> `Container` with:
      - `decoration: BoxDecoration(color: AppTheme.glassColor(context).withOpacity(opacity), borderRadius: BorderRadius.circular(borderRadius), border: Border.all(color: AppTheme.glassBorder.withOpacity(0.2)))`
      - `padding: padding`
    - Import `dart:ui` for `ImageFilter`
    - Wrap the entire widget in `RepaintBoundary` for performance (per RESEARCH.md Pitfall 5)

    **lib/theme/gradient_background.dart** (per UI-10):
    - `class GradientBackground extends StatelessWidget`
    - Constructor: `const GradientBackground({super.key, required this.child, this.variant = GradientVariant.primary})`
    - `enum GradientVariant { primary, secondary, accent }`
    - primary gradient: `LinearGradient(begin: Alignment.topLeft, end: Alignment.bottomRight, colors: [Color(0xFF1B5E20), Color(0xFF0D47A1), Color(0xFF1A237E)])` (green to blue to indigo)
    - secondary: `[Color(0xFF4A148C), Color(0xFF0D47A1), Color(0xFF00695C)]` (purple to blue to teal)
    - accent: `[Color(0xFFE65100), Color(0xFFBF360C), Color(0xFF4E342E)]` (orange to brown)
    - For dark mode, use same colors. For light mode, add a white overlay at 0.85 opacity on top
    - Build: `Stack(children: [Container(decoration: BoxDecoration(gradient: ...)), child])`
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "class AppTheme" lib/theme/app_theme.dart && grep -c "class GlassCard" lib/theme/glass_card.dart && grep -c "class GradientBackground" lib/theme/gradient_background.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/theme/app_theme.dart` contains `class AppTheme` (grep-verifiable)
    - File `lib/theme/app_theme.dart` contains `static ThemeData light()` (grep-verifiable)
    - File `lib/theme/app_theme.dart` contains `static ThemeData dark()` (grep-verifiable)
    - File `lib/theme/app_theme.dart` contains `GoogleFonts` (grep-verifiable)
    - File `lib/theme/app_theme.dart` contains `Color(0xFF2E7D32)` (grep-verifiable)
    - File `lib/theme/app_theme.dart` contains `glassLight` and `glassDark` and `glassBorder` (grep-verifiable)
    - File `lib/theme/glass_card.dart` contains `class GlassCard extends StatelessWidget` (grep-verifiable)
    - File `lib/theme/glass_card.dart` contains `BackdropFilter` (grep-verifiable)
    - File `lib/theme/glass_card.dart` contains `RepaintBoundary` (grep-verifiable)
    - File `lib/theme/glass_card.dart` contains `ClipRRect` (grep-verifiable)
    - File `lib/theme/glass_card.dart` contains `ImageFilter.blur` (grep-verifiable)
    - File `lib/theme/gradient_background.dart` contains `class GradientBackground` (grep-verifiable)
    - File `lib/theme/gradient_background.dart` contains `enum GradientVariant` (grep-verifiable)
    - File `lib/theme/gradient_background.dart` contains `LinearGradient` (grep-verifiable)
  </acceptance_criteria>
  <done>Design system foundation: AppTheme with light/dark modes, GlassCard with performance guardrails, GradientBackground with 3 variants</done>
</task>

<task type="auto">
  <name>Task 3: Reusable widgets — SkeletonLoader, ErrorState, EmptyState, AiProgressIndicator, TimelineNode, ConfidenceBadge</name>
  <files>
    lib/widgets/skeleton_loader.dart,
    lib/widgets/error_state.dart,
    lib/widgets/empty_state.dart,
    lib/widgets/ai_progress_indicator.dart,
    lib/widgets/timeline_node.dart,
    lib/widgets/confidence_badge.dart
  </files>
  <read_first>
    lib/theme/glass_card.dart,
    lib/theme/app_theme.dart,
    lib/models/roadmap.dart,
    .planning/phases/01-full-platform-overhaul/01-RESEARCH.md
  </read_first>
  <action>
    Create `lib/widgets/` directory with 6 files:

    **lib/widgets/skeleton_loader.dart** (per UI-05):
    - `class SkeletonLoader extends StatelessWidget`
    - Constructor: `const SkeletonLoader({super.key, this.lines = 3, this.hasAvatar = false, this.hasImage = false})`
    - Uses `shimmer` package: `Shimmer.fromColors(baseColor: Colors.grey[300]!, highlightColor: Colors.grey[100]!, child: ...)`
    - For dark mode: `baseColor: Colors.grey[800]!`, `highlightColor: Colors.grey[700]!`
    - Renders `lines` number of rounded rectangles (height: 16, varying widths: 100%, 80%, 60%), with optional circle avatar (48x48) and image block (height: 200)
    - Static factory: `SkeletonLoader.card()` — returns a skeleton inside a GlassCard shape (rounded rect container)
    - Static factory: `SkeletonLoader.list({int itemCount = 5})` — returns a Column of `itemCount` skeleton items with SizedBox(height: 12) separators

    **lib/widgets/error_state.dart** (per UI-09):
    - `class ErrorStateWidget extends StatelessWidget`
    - Constructor: `const ErrorStateWidget({super.key, required this.message, required this.onRetry, this.icon = Icons.error_outline})`
    - Renders a centered Column: Icon (64px, colorScheme.error), SizedBox(16), Text(message, textAlign center, style titleMedium), SizedBox(24), FilledButton.icon(onPressed: onRetry, icon: Icon(Icons.refresh), label: Text('Retry'))
    - Wrap in a GlassCard for visual consistency
    - Add `.animate()` from flutter_animate: `.fadeIn(duration: 300.ms).slideY(begin: 0.1, end: 0)`

    **lib/widgets/empty_state.dart** (per UI-09):
    - `class EmptyStateWidget extends StatelessWidget`
    - Constructor: `const EmptyStateWidget({super.key, required this.title, required this.subtitle, this.icon = Icons.inbox_outlined, this.actionLabel, this.onAction})`
    - Renders: Icon (80px, colorScheme.outline.withOpacity(0.5)), SizedBox(16), Text(title, style titleLarge), SizedBox(8), Text(subtitle, style bodyMedium, color onSurface.withOpacity(0.6), textAlign center), optional FilledButton.tonal if actionLabel is set
    - Add `.animate()`: `.fadeIn(duration: 400.ms).scale(begin: Offset(0.95, 0.95), end: Offset(1, 1))`

    **lib/widgets/ai_progress_indicator.dart** (per UI-07):
    - `class AiProgressIndicator extends StatelessWidget`
    - Constructor: `const AiProgressIndicator({super.key, required this.currentStep, this.totalSteps = 4})`
    - `currentStep` is 0-indexed (0=analyzing, 1=detecting, 2=building, 3=curating)
    - Static const `_steps` list: `['Analyzing your background...', 'Identifying skill gaps...', 'Building your roadmap...', 'Curating resources...']`
    - Static const `_icons` list: `[Icons.person_search, Icons.psychology, Icons.map_outlined, Icons.library_books]`
    - Renders a Column of 4 step rows, each with:
      - Leading: completed steps get `Icon(Icons.check_circle, color: Colors.green)`, active step gets a `SizedBox(24x24, child: CircularProgressIndicator(strokeWidth: 2))`, pending steps get `Icon(Icons.radio_button_unchecked, color: onSurface.withOpacity(0.3))`
      - Title: `Text(_steps[i])` with bold for active, normal for others
      - Trailing: `Icon(_icons[i])`
      - Vertical connector lines between rows (2px wide Container, height 16, color green for completed, grey for pending)
    - Active step row: wrap in `.animate().fadeIn().shimmer(duration: 1500.ms)` from flutter_animate
    - Completed step rows: `.animate().fadeIn(duration: 200.ms)`
    - Wrap the whole Column in a GlassCard

    **lib/widgets/timeline_node.dart** (per UI-06):
    - `class TimelineNode extends StatelessWidget`
    - Constructor: `const TimelineNode({super.key, required this.stage, required this.progress, required this.isFirst, required this.isLast, required this.isCurrent, required this.onProgressChanged})`
    - Parameters: `RoadmapStage stage`, `double progress` (0.0-1.0), `bool isFirst/isLast/isCurrent`, `ValueChanged<double> onProgressChanged`
    - Build: Row with 2 children:
      1. Timeline spine (left side, 48px wide): Column with:
         - Top connector: `Container(width: 2, height: 24, color: isFirst ? Colors.transparent : colorScheme.primary)`
         - Node circle: `Container(width: isCurrent ? 20 : 14, height: isCurrent ? 20 : 14, decoration: BoxDecoration(shape: BoxShape.circle, color: progress >= 1.0 ? Colors.green : isCurrent ? colorScheme.primary : colorScheme.outline.withOpacity(0.3)))` — if `isCurrent`, add a pulsing glow via `BoxShadow` and flutter_animate `.animate(onPlay: (c) => c.repeat()).scale(begin: Offset(1,1), end: Offset(1.2,1.2), duration: 1000.ms).then().scale(begin: Offset(1.2,1.2), end: Offset(1,1), duration: 1000.ms)`
         - Bottom connector: `Container(width: 2, flex: 1, color: isLast ? Colors.transparent : colorScheme.primary.withOpacity(0.3))`
      2. Milestone card (right side, Expanded): GlassCard containing:
         - `Text(stage.title, style: titleSmall)`
         - `Text("Level: ${stage.level.capitalize()}", style: bodySmall)`
         - If `stage.tasks.isNotEmpty`: ExpansionTile with task list as `CheckboxListTile` items
         - If `stage.resources.isNotEmpty`: Row of resource links
         - `Slider(value: progress, onChanged: onProgressChanged, min: 0, max: 1, divisions: 20)`
         - `Text("${(progress * 100).round()}% complete", style: bodySmall)`
    - Use flutter_animate: each node `.animate().fadeIn(delay: (index * 100).ms).slideX(begin: 0.1, end: 0)` — pass `index` as a separate param `this.index = 0`

    **lib/widgets/confidence_badge.dart** (per ADAPT-06):
    - `class ConfidenceBadge extends StatelessWidget`
    - Constructor: `const ConfidenceBadge({super.key, required this.confidence, this.label})`
    - `confidence` is 0.0-1.0
    - Color mapping: `confidence >= 0.8 -> Colors.green`, `>= 0.5 -> Colors.orange`, `< 0.5 -> Colors.red`
    - Label text: if `label` is null, show `"${(confidence * 100).round()}%"`
    - Renders: `Container` with `decoration: BoxDecoration(color: badgeColor.withOpacity(0.15), borderRadius: BorderRadius.circular(8), border: Border.all(color: badgeColor.withOpacity(0.5)))`, padding 4h/8v, child: `Text(labelText, style: TextStyle(color: badgeColor, fontWeight: FontWeight.bold, fontSize: 12))`
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && ls lib/widgets/ && grep -c "class SkeletonLoader" lib/widgets/skeleton_loader.dart && grep -c "class ErrorStateWidget" lib/widgets/error_state.dart && grep -c "class EmptyStateWidget" lib/widgets/empty_state.dart && grep -c "class AiProgressIndicator" lib/widgets/ai_progress_indicator.dart && grep -c "class TimelineNode" lib/widgets/timeline_node.dart && grep -c "class ConfidenceBadge" lib/widgets/confidence_badge.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/widgets/skeleton_loader.dart` contains `class SkeletonLoader` and `Shimmer.fromColors` (grep-verifiable)
    - File `lib/widgets/error_state.dart` contains `class ErrorStateWidget` and `onRetry` and `.animate()` (grep-verifiable)
    - File `lib/widgets/empty_state.dart` contains `class EmptyStateWidget` and `title` and `subtitle` (grep-verifiable)
    - File `lib/widgets/ai_progress_indicator.dart` contains `class AiProgressIndicator` and `Analyzing your background` and `Identifying skill gaps` and `Building your roadmap` and `Curating resources` (grep-verifiable)
    - File `lib/widgets/ai_progress_indicator.dart` contains `GlassCard` (grep-verifiable)
    - File `lib/widgets/timeline_node.dart` contains `class TimelineNode` and `RoadmapStage` and `GlassCard` and `Slider` (grep-verifiable)
    - File `lib/widgets/confidence_badge.dart` contains `class ConfidenceBadge` and `Colors.green` and `Colors.orange` and `Colors.red` (grep-verifiable)
    - All 6 widget files exist in `lib/widgets/`
  </acceptance_criteria>
  <done>All 6 reusable widgets created: skeleton loader with shimmer, error/empty states with animations, AI progress storytelling, timeline node with pulsing, confidence badge with color coding</done>
</task>

<task type="auto">
  <name>Task 4: Wire AppTheme into main.dart with dark mode toggle</name>
  <files>lib/main.dart</files>
  <read_first>
    lib/main.dart,
    lib/theme/app_theme.dart,
    lib/providers/app_services.dart
  </read_first>
  <action>
    Modify `lib/main.dart`:

    1. Add import: `import 'theme/app_theme.dart';`
    2. Add a `ValueNotifier<ThemeMode> themeMode = ValueNotifier(ThemeMode.dark);` as a top-level global (above `main()`)
    3. In `_PathwiseAppState.build()`, wrap the `MaterialApp` in a `ValueListenableBuilder<ThemeMode>(valueListenable: themeMode, builder: (context, mode, _) => ...)`
    4. Replace the existing `ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF2E7D32)), useMaterial3: true)` with:
       - `theme: AppTheme.light()`
       - `darkTheme: AppTheme.dark()`
       - `themeMode: mode`
    5. Keep ALL existing code structure (AppServices, MultiProvider, _AuthGate) exactly the same
    6. Do NOT add Dio/ApiClient yet (that happens in Plan 03)
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "AppTheme.light()" lib/main.dart && grep -c "AppTheme.dark()" lib/main.dart && grep -c "themeMode" lib/main.dart && grep -c "ValueNotifier<ThemeMode>" lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/main.dart` contains `import 'theme/app_theme.dart'` (grep-verifiable)
    - File `lib/main.dart` contains `AppTheme.light()` (grep-verifiable)
    - File `lib/main.dart` contains `AppTheme.dark()` (grep-verifiable)
    - File `lib/main.dart` contains `ValueNotifier<ThemeMode>` (grep-verifiable)
    - File `lib/main.dart` contains `ValueListenableBuilder` (grep-verifiable)
    - File `lib/main.dart` still contains `AppServices(` (existing service locator preserved)
    - File `lib/main.dart` still contains `MultiProvider(` (existing providers preserved)
    - File `lib/main.dart` still contains `_AuthGate` (existing auth gate preserved)
    - File `lib/main.dart` does NOT contain `ColorScheme.fromSeed(seedColor: const Color(0xFF2E7D32))` (old theme removed)
  </acceptance_criteria>
  <done>main.dart wired to AppTheme with dual light/dark mode support via ValueNotifier toggle, all existing architecture preserved</done>
</task>

<task type="auto">
  <name>Task 5: Overhaul core screens — LoginScreen, HomeModeScreen, UserMainShell with glassmorphism</name>
  <files>
    lib/screens/login_screen.dart,
    lib/screens/home_mode_screen.dart,
    lib/screens/user_main_shell.dart
  </files>
  <read_first>
    lib/screens/login_screen.dart,
    lib/screens/home_mode_screen.dart,
    lib/screens/user_main_shell.dart,
    lib/theme/glass_card.dart,
    lib/theme/gradient_background.dart,
    lib/theme/app_theme.dart,
    lib/main.dart
  </read_first>
  <action>
    **lib/screens/login_screen.dart** (per UI-01, UI-10, UI-04):
    - Wrap Scaffold body in `GradientBackground(variant: GradientVariant.primary, child: ...)`
    - Replace plain Text/Button with a centered GlassCard containing:
      - App logo (existing `assets/logo.png.jpeg`) in a CircleAvatar(radius: 48)
      - SizedBox(24)
      - Text("Pathwise", style: Theme.of(context).textTheme.headlineLarge)
      - Text("Your AI-powered career guide", style: bodyMedium, color onSurface.withOpacity(0.7))
      - SizedBox(32)
      - Google Sign-In button as `FilledButton.icon(icon: Icon(Icons.login), label: Text("Sign in with Google"), onPressed: existing auth logic)`
    - Add flutter_animate: logo `.animate().fadeIn(duration: 600.ms).scale(begin: Offset(0.8, 0.8))`, title `.animate().fadeIn(delay: 200.ms).slideY(begin: 0.2)`, button `.animate().fadeIn(delay: 400.ms).slideY(begin: 0.1)`
    - Keep existing `_signIn()` logic unchanged

    **lib/screens/home_mode_screen.dart** (per UI-01, UI-10, UI-04):
    - Wrap body in `GradientBackground(variant: GradientVariant.primary, child: ...)`
    - Replace plain ListTile/Card items with GlassCard widgets for each mode option (AI Guidance, Expert Consultation)
    - Each GlassCard: Row with Icon, Column with title+subtitle, trailing arrow icon
    - Add dark mode toggle: IconButton in AppBar actions area: `IconButton(icon: Icon(themeMode.value == ThemeMode.dark ? Icons.light_mode : Icons.dark_mode), onPressed: () { themeMode.value = themeMode.value == ThemeMode.dark ? ThemeMode.light : ThemeMode.dark; })`
    - Import `'../main.dart'` for the `themeMode` ValueNotifier
    - Add entrance animations: each GlassCard `.animate().fadeIn(delay: (index * 100).ms).slideX(begin: -0.1)`

    **lib/screens/user_main_shell.dart** (per UI-01):
    - Add dark mode toggle IconButton in the AppBar actions list (same pattern as home_mode_screen)
    - Update BottomNavigationBar styling: use `Theme.of(context).colorScheme` for selected/unselected colors
    - No structural changes to navigation logic
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "GradientBackground" lib/screens/login_screen.dart && grep -c "GlassCard" lib/screens/login_screen.dart && grep -c "GradientBackground" lib/screens/home_mode_screen.dart && grep -c "GlassCard" lib/screens/home_mode_screen.dart && grep -c "themeMode" lib/screens/home_mode_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/screens/login_screen.dart` contains `GradientBackground` and `GlassCard` (grep-verifiable)
    - File `lib/screens/login_screen.dart` contains `.animate()` (flutter_animate micro-interaction)
    - File `lib/screens/home_mode_screen.dart` contains `GradientBackground` and `GlassCard` (grep-verifiable)
    - File `lib/screens/home_mode_screen.dart` contains `themeMode` (dark mode toggle)
    - File `lib/screens/home_mode_screen.dart` contains `.animate()` (flutter_animate)
    - File `lib/screens/user_main_shell.dart` contains `themeMode` (dark mode toggle)
  </acceptance_criteria>
  <done>Login, Home, and Shell screens transformed with gradient backgrounds, glass cards, dark mode toggle, and entrance animations</done>
</task>

<task type="auto">
  <name>Task 6: Overhaul AiGuidanceScreen — multi-step onboarding wizard with progress indicator</name>
  <files>lib/screens/ai_guidance_screen.dart</files>
  <read_first>
    lib/screens/ai_guidance_screen.dart,
    lib/widgets/ai_progress_indicator.dart,
    lib/theme/glass_card.dart,
    lib/theme/gradient_background.dart,
    lib/providers/app_services.dart,
    lib/models/app_user.dart
  </read_first>
  <action>
    Refactor `lib/screens/ai_guidance_screen.dart` from a single-form screen into a 4-step wizard (per UI-02):

    **Step structure** — Use `PageView` with `PageController`:
    - Step 0: Career Goals — `TextField` for career goals, pre-filled from `widget.appUser.careerGoals`
    - Step 1: Skills — `TextField` with chip-add pattern (existing `_skillCtrl` + `_skills` list), display as `Wrap` of `Chip` widgets
    - Step 2: Resume — `TextField` for resume text + file picker button (existing `_pickResumeFile()`)
    - Step 3: Interests — `TextField` with chip-add pattern (existing `_interestCtrl` + `_interests` list)

    **Progress indicator** — At the top of the screen, show a Row of 4 step circles connected by lines:
    - Active step: `CircleAvatar(radius: 16, backgroundColor: colorScheme.primary, child: Text("${step+1}"))`
    - Completed: `CircleAvatar(radius: 16, backgroundColor: Colors.green, child: Icon(Icons.check, size: 16))`
    - Pending: `CircleAvatar(radius: 16, backgroundColor: colorScheme.outline.withOpacity(0.3), child: Text("${step+1}"))`
    - Connector lines: `Expanded(child: Container(height: 2, color: stepCompleted ? Colors.green : colorScheme.outline.withOpacity(0.3)))`
    - Step labels below circles: ["Goals", "Skills", "Resume", "Interests"]

    **Navigation** — Bottom row with Back/Next buttons:
    - Back: `OutlinedButton(onPressed: step > 0 ? _previousStep : null, child: Text("Back"))`
    - Next: `FilledButton(onPressed: _nextStep, child: Text(step == 3 ? "Generate Roadmap" : "Next"))`
    - PageView transitions: `pageController.animateToPage(step, duration: Duration(milliseconds: 300), curve: Curves.easeInOut)`

    **AI Analysis state** — When "Generate Roadmap" is pressed:
    - Set `_working = true`
    - Show AiProgressIndicator widget (from lib/widgets/) in a modal/overlay instead of the wizard
    - Keep existing `svc.ai.analyze(...)` call (will be replaced with ApiClient in Plan 03)
    - After analysis, navigate to RoadmapDetailScreen (existing behavior)

    **Styling:**
    - Wrap Scaffold body in `GradientBackground(variant: GradientVariant.secondary)`
    - Each step page content inside a GlassCard
    - Add page transition animation: each step content `.animate().fadeIn(duration: 300.ms).slideX(begin: 0.05)`

    Keep ALL existing controller logic (_resumeCtrl, _goalsCtrl, _skillCtrl, _interestCtrl, _skills, _interests, _pickResumeFile). Reorganize the existing single form into 4 pages in the PageView.
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "PageView\|PageController" lib/screens/ai_guidance_screen.dart && grep -c "AiProgressIndicator" lib/screens/ai_guidance_screen.dart && grep -c "GradientBackground" lib/screens/ai_guidance_screen.dart && grep -c "GlassCard" lib/screens/ai_guidance_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/screens/ai_guidance_screen.dart` contains `PageView` or `PageController` (wizard navigation)
    - File `lib/screens/ai_guidance_screen.dart` contains `AiProgressIndicator` (4-step AI progress storytelling)
    - File `lib/screens/ai_guidance_screen.dart` contains `GradientBackground` (styled background)
    - File `lib/screens/ai_guidance_screen.dart` contains `GlassCard` (glass card styling)
    - File `lib/screens/ai_guidance_screen.dart` contains `Goals` and `Skills` and `Resume` and `Interests` (step labels)
    - File `lib/screens/ai_guidance_screen.dart` contains `.animate()` (flutter_animate transitions)
    - File `lib/screens/ai_guidance_screen.dart` still contains `_pickResumeFile` (existing functionality preserved)
    - File `lib/screens/ai_guidance_screen.dart` still contains `svc.ai.analyze` or `context.svc.ai` (existing AI call preserved for now)
  </acceptance_criteria>
  <done>AiGuidanceScreen refactored into 4-step onboarding wizard with animated page transitions, progress indicator, AI progress storytelling overlay, and glassmorphism styling</done>
</task>

<task type="auto">
  <name>Task 7: Overhaul RoadmapDetailScreen — vertical timeline with connected nodes</name>
  <files>lib/screens/roadmap_detail_screen.dart</files>
  <read_first>
    lib/screens/roadmap_detail_screen.dart,
    lib/widgets/timeline_node.dart,
    lib/widgets/skeleton_loader.dart,
    lib/widgets/error_state.dart,
    lib/widgets/confidence_badge.dart,
    lib/theme/glass_card.dart,
    lib/theme/gradient_background.dart,
    lib/models/roadmap.dart
  </read_first>
  <action>
    Rewrite `lib/screens/roadmap_detail_screen.dart` body (per UI-06, UI-05, UI-09, ADAPT-06):

    **Loading state:** Replace `Center(child: CircularProgressIndicator())` with `SkeletonLoader.list(itemCount: 3)` wrapped in a `GradientBackground`

    **Error state:** If StreamBuilder has error, show `ErrorStateWidget(message: "Failed to load roadmap", onRetry: () => setState(() {}))` — trigger rebuild

    **Empty state:** If data is null or milestones empty, show `EmptyStateWidget(title: "No Roadmap Yet", subtitle: "Complete the AI guidance wizard to generate your personalized roadmap", icon: Icons.map_outlined)`

    **Main body** — Replace the existing `_RoadmapBody` widget:
    - Wrap in `GradientBackground(variant: GradientVariant.primary)`
    - Top section: GlassCard with `Text(roadmap.targetRole, style: headlineSmall)`, `Text(roadmap.timeline, style: bodyMedium)`, overall progress ring (CircularProgressIndicator with calculated average of all stageProgress values)
    - Timeline section: `ListView.builder` with `itemCount: roadmap.structuredStages.length`
    - Each item: `TimelineNode(stage: stages[i], progress: roadmap.stageProgress[stages[i].level] ?? 0.0, isFirst: i == 0, isLast: i == stages.length - 1, isCurrent: _isCurrentStage(i, stages, roadmap.stageProgress), onProgressChanged: (val) => _updateProgress(roadmap, stages[i].level, val), index: i)`
    - `_isCurrentStage`: returns true for the first stage with progress < 1.0
    - `_updateProgress`: calls `svc.roadmaps.updateProgress(roadmap.id, {stage.level: val})` (existing repository method)
    - If roadmap has `skillGaps` field (new AI-generated roadmaps), display skill gaps as a Row of `ConfidenceBadge` widgets — read from `snap.data!.data()!['skillGaps']` and check if it exists (progressive enhancement)

    **Animations:**
    - Overall progress ring: `.animate().fadeIn(duration: 500.ms).scale(begin: Offset(0.9, 0.9))`
    - Timeline nodes get staggered entrance via the `index` parameter on TimelineNode
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "TimelineNode" lib/screens/roadmap_detail_screen.dart && grep -c "SkeletonLoader" lib/screens/roadmap_detail_screen.dart && grep -c "ErrorStateWidget" lib/screens/roadmap_detail_screen.dart && grep -c "GradientBackground" lib/screens/roadmap_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/screens/roadmap_detail_screen.dart` contains `TimelineNode` (timeline visualization)
    - File `lib/screens/roadmap_detail_screen.dart` contains `SkeletonLoader` (skeleton loading)
    - File `lib/screens/roadmap_detail_screen.dart` contains `ErrorStateWidget` (error state)
    - File `lib/screens/roadmap_detail_screen.dart` contains `EmptyStateWidget` (empty state)
    - File `lib/screens/roadmap_detail_screen.dart` contains `GradientBackground` (styled background)
    - File `lib/screens/roadmap_detail_screen.dart` contains `GlassCard` (glass cards)
    - File `lib/screens/roadmap_detail_screen.dart` contains `ConfidenceBadge` (skill gap confidence)
    - File `lib/screens/roadmap_detail_screen.dart` contains `.animate()` (micro-interactions)
    - File `lib/screens/roadmap_detail_screen.dart` still contains `StreamBuilder` (existing data pattern preserved)
  </acceptance_criteria>
  <done>RoadmapDetailScreen transformed into vertical timeline with connected nodes, pulsing current position, skeleton loading, error/empty states, confidence badges, and glassmorphism styling</done>
</task>

<task type="auto">
  <name>Task 8: Overhaul ExpertsScreen — filters, sorting, and marketplace polish</name>
  <files>
    lib/screens/experts_screen.dart,
    lib/screens/expert_detail_screen.dart,
    lib/screens/book_consultation_screen.dart
  </files>
  <read_first>
    lib/screens/experts_screen.dart,
    lib/screens/expert_detail_screen.dart,
    lib/screens/book_consultation_screen.dart,
    lib/models/expert.dart,
    lib/models/consultation.dart,
    lib/theme/glass_card.dart,
    lib/widgets/skeleton_loader.dart,
    lib/widgets/error_state.dart,
    lib/widgets/empty_state.dart
  </read_first>
  <action>
    **lib/screens/experts_screen.dart** (per UI-08, EXP-01, UI-01, UI-05, UI-09):
    - Add state: `String? _selectedDomain`, `RangeValues _priceRange = RangeValues(0, 10000)`, `double _minRating = 0`, `String _sortBy = 'rating'` (options: 'rating', 'price_low', 'price_high', 'reviews')
    - Convert from StatelessWidget to StatefulWidget
    - Add filter bar below AppBar (SliverPersistentHeader or Column before list):
      - Domain filter chips: `Wrap` of `FilterChip` widgets. Extract unique domains from the expert list. Include an "All" chip. `onSelected` sets `_selectedDomain`
      - Price range slider: `RangeSlider(values: _priceRange, min: 0, max: 10000, divisions: 20, labels: RangeLabels("INR ${_priceRange.start.round()}", "INR ${_priceRange.end.round()}"), onChanged: (v) => setState(() => _priceRange = v))`
      - Rating filter: `Row` with `Text("Min rating:")` and 5 star icons (tappable, sets `_minRating`)
      - Sort dropdown: `DropdownButton<String>(value: _sortBy, items: [rating, price_low, price_high, reviews], onChanged: ...)`
    - Apply filters to the StreamBuilder data in builder:
      ```dart
      var filtered = list.where((e) => e.isVerified).where((e) =>
        (_selectedDomain == null || e.domain == _selectedDomain) &&
        e.pricePerSession >= _priceRange.start &&
        e.pricePerSession <= _priceRange.end &&
        e.rating >= _minRating
      ).toList();
      // Sort
      if (_sortBy == 'rating') filtered.sort((a, b) => b.rating.compareTo(a.rating));
      else if (_sortBy == 'price_low') filtered.sort((a, b) => a.pricePerSession.compareTo(b.pricePerSession));
      else if (_sortBy == 'price_high') filtered.sort((a, b) => b.pricePerSession.compareTo(a.pricePerSession));
      else if (_sortBy == 'reviews') filtered.sort((a, b) => b.totalReviews.compareTo(a.totalReviews));
      ```
    - Replace loading with `SkeletonLoader.list(itemCount: 4)`
    - Replace empty text with `EmptyStateWidget(title: "No Experts Found", subtitle: "Try adjusting your filters")`
    - Replace `ListTile` with GlassCard per expert: Row with `CircleAvatar(child: Text(expert.name[0]))`, Column(name, domain, rating stars, price), trailing arrow
    - Each card: `.animate().fadeIn(delay: (index * 80).ms).slideY(begin: 0.05)`

    **lib/screens/expert_detail_screen.dart** (per EXP-02, UI-01, UI-05, UI-09):
    - Wrap in `GradientBackground(variant: GradientVariant.accent)`
    - Header: GlassCard with expert name, domain, verified badge, rating row (star icons + Text)
    - Skills section: `Wrap` of `Chip` widgets from `expert.skills`
    - Experience section: Text(expert.experience)
    - Reviews section: StreamBuilder for reviews (existing), each review in GlassCard
    - Replace loading with `SkeletonLoader(lines: 5, hasAvatar: true)`
    - Replace error text with `ErrorStateWidget`
    - Add book consultation button: `FilledButton.icon(icon: Icon(Icons.calendar_today), label: Text("Book Consultation - INR ${expert.pricePerSession}"))`

    **lib/screens/book_consultation_screen.dart** (per EXP-03, UI-01):
    - Add glassmorphism: GradientBackground + GlassCard wrapping form fields
    - Clear type selection: `SegmentedButton` or `ChoiceChip` row for consultation types (instead of plain dropdown if applicable)
    - Pricing display: large `Text("INR ${price}", style: headlineMedium)` in a GlassCard
    - Submit button with spring feedback: `.animate().scale(begin: Offset(1,1), end: Offset(0.95, 0.95), duration: 100.ms).then().scale(begin: Offset(0.95,0.95), end: Offset(1,1), duration: 100.ms)` on tap
    - Replace loading with `SkeletonLoader(lines: 3)`
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "FilterChip\|RangeSlider\|DropdownButton" lib/screens/experts_screen.dart && grep -c "GlassCard" lib/screens/experts_screen.dart && grep -c "SkeletonLoader" lib/screens/experts_screen.dart && grep -c "GlassCard" lib/screens/expert_detail_screen.dart && grep -c "GlassCard" lib/screens/book_consultation_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/screens/experts_screen.dart` contains `FilterChip` (domain filter chips)
    - File `lib/screens/experts_screen.dart` contains `RangeSlider` (price range filter)
    - File `lib/screens/experts_screen.dart` contains `_minRating` (rating filter)
    - File `lib/screens/experts_screen.dart` contains `_sortBy` (sort options)
    - File `lib/screens/experts_screen.dart` contains `GlassCard` (glassmorphism)
    - File `lib/screens/experts_screen.dart` contains `SkeletonLoader` (skeleton loading)
    - File `lib/screens/experts_screen.dart` contains `EmptyStateWidget` (empty state)
    - File `lib/screens/experts_screen.dart` contains `.animate()` (entrance animations)
    - File `lib/screens/expert_detail_screen.dart` contains `GradientBackground` and `GlassCard` (glassmorphism)
    - File `lib/screens/expert_detail_screen.dart` contains `SkeletonLoader` (skeleton loading)
    - File `lib/screens/book_consultation_screen.dart` contains `GlassCard` (glassmorphism)
  </acceptance_criteria>
  <done>Expert marketplace overhauled with domain filter chips, price range slider, rating filter, sort options, expert profile with full skill display and reviews, consultation booking with clear pricing</done>
</task>

<task type="auto">
  <name>Task 9: Overhaul remaining screens — ConsultationDetail, ExpertHome, Profile, ReviewSubmit, AdminDashboard, RoleRouter</name>
  <files>
    lib/screens/consultation_detail_screen.dart,
    lib/screens/expert_home_screen.dart,
    lib/screens/profile_screen.dart,
    lib/screens/review_submit_screen.dart,
    lib/screens/admin_dashboard_screen.dart,
    lib/screens/role_router.dart
  </files>
  <read_first>
    lib/screens/consultation_detail_screen.dart,
    lib/screens/expert_home_screen.dart,
    lib/screens/profile_screen.dart,
    lib/screens/review_submit_screen.dart,
    lib/screens/admin_dashboard_screen.dart,
    lib/screens/role_router.dart,
    lib/theme/glass_card.dart,
    lib/theme/gradient_background.dart,
    lib/widgets/skeleton_loader.dart,
    lib/widgets/error_state.dart,
    lib/widgets/empty_state.dart
  </read_first>
  <action>
    Apply glassmorphism, skeleton loading, error/empty states, and animations to all remaining screens. For each screen, the pattern is:
    1. Import glass_card, gradient_background, skeleton_loader, error_state, empty_state, flutter_animate
    2. Wrap body in GradientBackground (where appropriate for key screens) or use scaffold backgroundColor from theme
    3. Replace CircularProgressIndicator with SkeletonLoader
    4. Replace empty Text with EmptyStateWidget
    5. Add error handling with ErrorStateWidget
    6. Replace Card/ListTile with GlassCard where impactful
    7. Add entrance animations with `.animate().fadeIn()`

    **lib/screens/consultation_detail_screen.dart** (per UI-01, UI-05, UI-09):
    - Loading: `SkeletonLoader(lines: 4, hasAvatar: true)`
    - Content: GlassCard wrapping consultation details (type, status, scheduled date, price)
    - Status badge: colored Container with status text (pending=orange, accepted=green, cancelled=red)
    - Payment button: existing logic unchanged, wrap in GlassCard with price display
    - Error state for StreamBuilder errors

    **lib/screens/expert_home_screen.dart** (per UI-01, UI-05, UI-09):
    - Loading: `SkeletonLoader.list(itemCount: 3)`
    - Empty: `EmptyStateWidget(title: "No Bookings Yet", subtitle: "Your consultation requests will appear here")`
    - Each booking: GlassCard with learner name, consultation type, status, date
    - Entrance animations per booking card

    **lib/screens/profile_screen.dart** (per UI-01, UI-04):
    - Wrap in GradientBackground(variant: GradientVariant.secondary)
    - User info in a large GlassCard: avatar, name, email, role badge
    - Skills as Wrap of Chips inside GlassCard
    - Interests as Wrap of Chips inside GlassCard
    - Dark mode toggle in a settings-style ListTile
    - Sign out button: `OutlinedButton.icon(icon: Icon(Icons.logout), label: Text("Sign Out"))`
    - Entrance animation on the profile card

    **lib/screens/review_submit_screen.dart** (per UI-01, UI-04):
    - Wrap form in GlassCard
    - Star rating: Row of 5 `IconButton(icon: Icon(i <= _rating ? Icons.star : Icons.star_border, color: Colors.amber))`
    - Submit button with spring animation feedback
    - Success: show a celebration-style animation (green check icon that scales up with flutter_animate)

    **lib/screens/admin_dashboard_screen.dart** (per UI-01, UI-05, UI-09):
    - Wrap in GradientBackground(variant: GradientVariant.accent)
    - Replace tab content loading states with SkeletonLoader
    - User management cards: GlassCard per user
    - Expert verification cards: GlassCard with approval/rejection buttons
    - Review moderation: GlassCard per review with flag/delete actions
    - Empty states for each tab

    **lib/screens/role_router.dart** (per UI-05):
    - Replace `CircularProgressIndicator()` loading state with `SkeletonLoader(lines: 2, hasAvatar: true)` centered in a Scaffold
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && for f in consultation_detail_screen expert_home_screen profile_screen review_submit_screen admin_dashboard_screen role_router; do echo "$f: $(grep -c 'GlassCard\|SkeletonLoader\|ErrorStateWidget\|EmptyStateWidget' lib/screens/${f}.dart) widget refs"; done</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/screens/consultation_detail_screen.dart` contains `GlassCard` and `SkeletonLoader` (grep-verifiable)
    - File `lib/screens/expert_home_screen.dart` contains `GlassCard` and `SkeletonLoader` and `EmptyStateWidget` (grep-verifiable)
    - File `lib/screens/profile_screen.dart` contains `GradientBackground` and `GlassCard` (grep-verifiable)
    - File `lib/screens/profile_screen.dart` contains `themeMode` (dark mode toggle in profile)
    - File `lib/screens/review_submit_screen.dart` contains `GlassCard` (grep-verifiable)
    - File `lib/screens/review_submit_screen.dart` contains `.animate()` (celebration animation)
    - File `lib/screens/admin_dashboard_screen.dart` contains `GradientBackground` and `GlassCard` and `SkeletonLoader` (grep-verifiable)
    - File `lib/screens/admin_dashboard_screen.dart` contains `EmptyStateWidget` (empty tabs)
    - File `lib/screens/role_router.dart` contains `SkeletonLoader` (replaces CircularProgressIndicator)
    - None of the 6 files contain bare `CircularProgressIndicator()` as the only loading indicator (replaced by SkeletonLoader)
  </acceptance_criteria>
  <done>All remaining 6 screens overhauled with glassmorphism cards, skeleton loading, error/empty states, animations, and dark mode support</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Visual verification -- check glassmorphism, dark mode, animations across screens</name>
  <files>lib/screens/login_screen.dart, lib/screens/home_mode_screen.dart, lib/screens/ai_guidance_screen.dart, lib/screens/roadmap_detail_screen.dart, lib/screens/experts_screen.dart</files>
  <action>
    Human verifies the complete Flutter UI overhaul visually:
    1. Run the app: flutter run (or via IDE)
    2. Login screen: Verify gradient background, glass card with logo, animated entrance
    3. Toggle dark mode via the icon button in home screen -- verify all screens switch consistently
    4. Navigate to AI Guidance: Verify 4-step wizard with page transitions and progress indicator
    5. Trigger AI analysis: Verify 4-step progress storytelling animation appears
    6. View a roadmap: Verify vertical timeline with connected nodes and pulsing current position
    7. Navigate to Experts: Verify domain filter chips, price range slider, rating filter, sort dropdown work
    8. View expert profile: Verify skills chips, reviews, gradient background
    9. Check any loading state: Verify shimmer skeleton loaders instead of spinners
    10. Force an error (e.g., disable network): Verify error state with retry button appears
    11. Check admin dashboard: Verify gradient background and glass cards
    12. Check profile screen: Verify dark mode toggle, glass cards, sign out button
  </action>
  <verify>
    <automated>cd "C:/Users/offic/OneDrive/Desktop/Pathwise/PathWise-Mobile-App" && grep -c "GlassCard" lib/screens/login_screen.dart lib/screens/home_mode_screen.dart lib/screens/experts_screen.dart lib/screens/roadmap_detail_screen.dart lib/screens/admin_dashboard_screen.dart</automated>
  </verify>
  <done>All 14 screens visually confirmed: glassmorphism renders, dark mode toggles correctly, animations play, skeleton loading appears, error/empty states show. User types "approved".</done>
</task>


</tasks>

<verification>
1. `grep -r "GlassCard" lib/screens/ | wc -l` — should show GlassCard usage across multiple screens (aim for 10+)
2. `grep -r "SkeletonLoader" lib/screens/ | wc -l` — should show skeleton loader on all data-loading screens
3. `grep -r "ErrorStateWidget" lib/screens/ | wc -l` — should show error states on all StreamBuilder screens
4. `grep -r "EmptyStateWidget" lib/screens/ | wc -l` — should show empty states where applicable
5. `grep -r "GradientBackground" lib/screens/ | wc -l` — should show gradient backgrounds on key screens (4+)
6. `grep -r "\.animate()" lib/screens/ | wc -l` — should show flutter_animate usage across screens
7. `grep -r "CircularProgressIndicator()" lib/screens/` — should return empty or minimal (all replaced)
8. `grep "AppTheme" lib/main.dart` — theme wired in main
9. `grep "themeMode" lib/screens/ | wc -l` — dark mode toggle accessible from multiple screens
</verification>

<success_criteria>
- All 14 screens render with glassmorphism cards (BackdropFilter) and gradient mesh backgrounds
- Dark mode toggle works from home screen and profile screen, applies consistently
- Onboarding wizard has 4 animated steps with progress indicator
- Roadmap displays as vertical timeline with connected nodes and pulsing current position
- AI analysis shows 4-step progress storytelling with animated step transitions
- Expert marketplace has domain filter chips, price range slider, rating threshold, and sort options
- Skeleton shimmer loading replaces all CircularProgressIndicator loading states
- Error states with retry button on all screens with data loading
- Empty states with guidance text on all screens that can be empty
- Confidence badges show colored indicators (red/yellow/green) next to skill gaps
- Micro-interactions: card entrance animations, button spring feedback, progress ring fills
- No CircularProgressIndicator() used as sole loading indicator anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-full-platform-overhaul/01-02-SUMMARY.md`
</output>
